# Your Supabase or Neon Postgres connection string
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# Only matters once you deploy — locally, *.localhost is detected
# automatically by middleware.ts regardless of this value.
NEXT_PUBLIC_ROOT_DOMAIN="myplatform.com"
